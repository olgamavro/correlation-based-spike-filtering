[project]
name = "dynapse-experiments"
version = "0.1.0"
description = "Correlation-based spike filtering experiments on DYNAP-SE1"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"